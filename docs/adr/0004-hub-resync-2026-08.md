---
status: accepted
version: 1.1
updated: 2026-08-18
ai-generated: true
type: adr
scope: hub-sync
issue: "https://github.com/G-Ivan-A/mango_ba_prompts/issues/265"
related_prs:
  - "https://github.com/G-Ivan-A/mango_ba_prompts/pull/266"
source_hub: "https://github.com/G-Ivan-A/hybrid-Intelligence-lab"
source_sha: "3bfa4103c9efbbd59bc951814884920e406982e2"
---

# ADR-0004: Ре-синк базовых стандартов Хаба воспроизводимым скриптом

> **Статус:** Accepted · **Дата:** 2026-08-17 · **Issue:**
> [#265](https://github.com/G-Ivan-A/mango_ba_prompts/issues/265)

## Контекст

Последний синк спицы выполнен 2026-06-13 на Хаб `b683341`
([issue #72](https://github.com/G-Ivan-A/mango_ba_prompts/issues/72)). К
2026-08-17 Хаб ушёл вперёд на 610 коммитов, а в споке накопились два следствия
дрейфа:

1. **Битые ссылки.** `standards/GLOSSARY.md` был скопирован из Хаба вместе с
   hub-относительными ссылками (`../CONCEPT.md`, `../governance/REPO_MODEL.md`,
   `../governance/proposals/rfc-*.md`) — в споке этих целей нет и не было
   никогда. Хаб с тех пор ещё и переструктурировался: `CONCEPT.md` стал
   `docs/concept.md`, `governance/REPO_MODEL.md` — `pr-ops/repo-model.md`,
   `governance/proposals/` — `docs/rfc/`. Дефект относится к классу
   «некритично скопированный контекст» (§4.2 анализа готовности Хаба):
   документ уверенно ссылается на несуществующие цели, что поведенчески
   неотличимо от галлюцинации.
2. **Отсутствующий словарь.** Спица не знала терминов ADR-010 (Operating Mode,
   Экспертное исполнение, Абсолютные границы, Легальный выход, Task Type,
   Method), хотя задачи спице ставятся именно в этих терминах — включая эту.

Ручное копирование воспроизводит дефект 1 при каждом следующем синке: ссылки
переносятся как есть, а проверить их глазами в 60-килобайтном глоссарии
невозможно.

## Решение

1. **Синк выполняется скриптом, а не руками.**
   [`scripts/sync_from_hub.py`](../../scripts/sync_from_hub.py) переносит файлы
   по декларативному манифесту и переписывает каждую относительную ссылку по
   двум правилам:
   - цель тоже перенесена → относительный путь внутри спицы;
   - цель осталась в Хабе → permalink Хаба, закреплённый за SHA синка
     (`/blob/<sha>/…` или `/tree/<sha>/…`).

   Ссылка, которая не разрешается **и в Хабе**, останавливает синк с
   диагностикой: это дефект Хаба, и правится он в Хабе, а не в копии.

2. **Прямые hub-относительные ссылки в споке запрещены.** `../../CONCEPT.md`
   ломается при любом изменении структуры Хаба и не проверяется локально.
   Допустимы либо локальный путь, либо полный URL с закреплённым SHA.

3. **Рабочие копии локально не редактируются.** Файл из манифеста несёт баннер
   «рабочая копия» и frontmatter `source_hub` / `source_sha` /
   `source_of_truth` / `sync_policy`. Расхождение устраняется следующим синком;
   правка копии на месте молча создаёт форк нормы. Режим
   `sync_from_hub.py --check` делает расхождение проверяемым.

4. **Одна точка синка.** Все копии закреплены за одним `source_sha`, равным
   `last_sync.hub_sha` в [`.hub-profile.json`](../../.hub-profile.json).
   Частично обновлённый набор — состояние, которое нельзя воспроизвести.
   Прошлые точки синка переехали в `sync_history`, чтобы регрессионные проверки
   прошлых issue (issue #72) не запрещали следующий синк.

5. **Проверка в CI.**
   [`scripts/validate_issue_265_hub_sync.py`](../../scripts/validate_issue_265_hub_sync.py)
   требует нуля битых относительных ссылок по всему репозиторию, единого
   `source_sha` у рабочих копий и отсутствия путей, выходящих за корень
   репозитория. Инлайн-код и fenced-блоки не проверяются: там ссылка —
   синтаксический пример (`[KB: <slug>](kb/<path>#<anchor>)`), а не адрес.

## Что перенесено и что нет

Отбор сделан по §4.4 анализа готовности Хаба и отфильтрован видением спицы
(issue #263): спица — проект основателя для BA в Mango (телеком), ей нужна
методология исполнения прогона, а не общерепозиторное устройство Хаба.

| Перенесено | Зачем спице |
| :--- | :--- |
| `ai-rules/README.md`, `agent-work-rules.md`, `agent-onboarding-protocol.md`, `adversarial-stress-testing.md` | Правила поведения агента-исполнителя прогона; словарь Operating Mode / Method, в котором ставятся задачи. |
| `ai-governance/README.md`, `ai-governance.md`, `agent-security-checklist.md` | Периметр безопасности при работе с приватными данными Mango (актуально перед переводом репозитория в Private). |
| `standards/GLOSSARY.md` (из `standards/glossary.md` Хаба) | Canonical-словарь; заменяет копию с 23 битыми ссылками. Историческое имя файла сохранено: на него ссылаются `prompts/` и `standards/` спицы. |
| `standards/evals-contract-standard.md`, `analysis-standard.md`, `research-standard.md` | Стандарты, на которые опираются артефакты и прогоны спицы. |

| Не перенесено | Почему |
| :--- | :--- |
| `standards/README.md` (Хаба) | Реестр стандартов **Хаба**: половина строк — файлы, которых в споке нет. Перенос вернул бы битые ссылки. У спицы свой реестр: [`standards/README.md`](../../standards/README.md). |
| `standards/frontmatter-standard.md`, `frontmatter-docs-standard.md`, `file-naming.md`, `standard-meta-structure.md`, `adr-structure-standard.md`, `rfc-structure-standard.md` | У спицы уже есть собственная норма именования и сложившаяся практика frontmatter. Дублирование нормы создаёт два источника истины; ссылка на Хаб дешевле копии. |
| `tools/sync-from-hub.sh` (клиент Smart Sync Хаба) | ADR-009 запрещает вводить Smart Sync упреждающе — только под доказанной операционной болью. Здесь достаточно собственного скрипта и проверки. |
| `evals/` | Отдельная задача (T-07), вне границ T-01. |

## Приведение структуры каталогов к базовой структуре Хаба

Ре-синк перенёс методологию Хаба в `ai-rules/` и `ai-governance/`, но локальный
каталог `governance/` остался от структуры до Хаба: в нём одновременно лежали
операционные записи спицы, RFC, аудиты и старый протокол онбординга. Два
параллельных места для одного класса артефактов — источник расхождения ссылок
при следующем синке.

Каталог `governance/` расформирован; целевая структура — базовая структура Хаба
(`ai-governance/`, `ai-rules/`, `pr-ops/`, `docs/adr/`, `docs/rfc/`,
`standards/`):

| Было | Стало | Почему |
| :--- | :--- | :--- |
| `governance/artifact-map.md`, `BACKLOG.md`, `session-digests.md`, `migration-manifest.md`, `migration-issues-registry.md`, `migration-phase1-issues.md`, `sync-matrix-2026-06-17.md`, `prompt-feedback.json` | `pr-ops/` | Внутренние операционные записи репозитория — в Хабе этот класс живёт в `pr-ops/`. |
| `governance/agent-onboarding-protocol.md` (+ `.executable.md`) | `.archive/ai-rules/agent-onboarding-protocol_old.md` | Локальная адаптация v1.2 сохраняется рядом с рабочей копией Хаба v1.5 под суффиксом `_old`; актуализация до v1.5 — отдельная задача, в этом PR не выполняется. |
| `governance/rfc-*.md`, `governance/rfc/`, `governance/knowledge-transfer-to-hub/` | `docs/rfc/` | Базовая структура Хаба держит RFC в `docs/rfc/`. |
| `governance/audit-*.md` | `docs/audit/` | Аудиты — исторические документы, не действующая норма; отделены от операционных записей. |
| `governance/prompt-debugging-process.md` | `standards/` | Это действующий стандарт процесса спицы, а не governance-запись. |

Каталог `research/` **не создаётся**: он специфичен для Хаба, research-материалы
остаются reference-only через
[`docs/hub-research-dependencies.md`](../hub-research-dependencies.md).

Перенос выполнен воспроизводимо скриптом
[`experiments/restructure_governance_dirs.py`](../../experiments/restructure_governance_dirs.py):
`git mv` по декларативной таблице, переписывание путей по всему репозиторию и
пересчёт глубины относительных ссылок у перенесённых файлов. Permalink'и Хаба
(`.../blob/<sha>/governance/...`) при этом не трогаются: в Хабе на старых SHA
каталог `governance/` существовал, и переписывание URL уничтожило бы
traceability. Итог прогона: 22 перенесённых пути, 63 файла с переписанными
путями, 123 пересчитанные относительные ссылки.

## Сверка манифеста синка с итоговым видением T-00

[Issue #263](https://github.com/G-Ivan-A/mango_ba_prompts/issues/263) (T-00)
замержена [PR #264](https://github.com/G-Ivan-A/mango_ba_prompts/pull/264)
2026-08-17. Манифест `MANIFEST` в
[`scripts/sync_from_hub.py`](../../scripts/sync_from_hub.py) сверен с итоговым
(замерженным, а не черновым) текстом видения:

| Норма T-00 | Что требует от манифеста | Состояние |
| :--- | :--- | :--- |
| «GitHub + AI-исполнитель; серверной инфраструктуры и инструментального слоя здесь нет» (`README.md`, «Роль проекта и границы») | не переносить клиент Smart Sync и инструментарий Хаба | ✅ `tools/sync-from-hub.sh` в манифесте отсутствует |
| Research Хаба — reference-only через `docs/hub-research-dependencies.md`, в спицу не копируется | ни одного `research/*` в манифесте | ✅ отсутствует; `research/` как каталог не создаётся |
| «Качество системы исполнения > стоимость»: операция не завершена без механизма проверки (чек-лист / метрика evals / human-in-the-loop) | стандарты, задающие механизм проверки, должны быть под рукой | ✅ `standards/evals-contract-standard.md`, `analysis-standard.md`, `research-standard.md` |
| Задачи ставятся в терминах ADR-010 (Operating Mode, Method, Абсолютные границы) | canonical-словарь | ✅ `standards/glossary.md` → `standards/GLOSSARY.md` |
| Целевая видимость Private, приватные данные Mango | периметр безопасности | ✅ `ai-governance/` (3 файла) |
| T-00 прямо оставляет синк стандартов Хаба задаче T-01 | манифест не должен дублировать артефакты T-00 | ✅ пересечения по файлам с PR #264 нет |

**Результат сверки: расхождений не найдено, манифест не изменялся.** Состав из
11 записей соответствует границам итогового T-00; ни одна запись не переносит
то, что T-00 объявил вне периметра спицы.

## Последствия

**Положительные:**

- Дрейф в 610 коммитов устранён: все рабочие копии на `3bfa410`.
- Битые относительные ссылки устранены полностью и удерживаются проверкой в CI.
- Спица получила словарь ADR-010, в терминах которого ей ставятся задачи.
- Следующий синк — одна команда, а не ручная сверка ссылок.

**Риски и ограничения:**

- Скрипт требует локальный клон Хаба (сети из CI не предполагается): в CI
  работает только проверка ссылок и единства `source_sha`, а не сверка с Хабом.
- Permalink на SHA не следует за развитием Хаба: ссылка остаётся корректной, но
  указывает на снимок. Это осознанный обмен точности на воспроизводимость.
- Расхождение обнаруживается только при запуске синка человеком: синхронизация
  односторонняя и неавтоматическая (ADR-009).

## Альтернативы

- **Починить 23 ссылки в старой копии глоссария руками.** Отклонено: устраняет
  симптом, оставляет дрейф и не даёт словарь ADR-010; следующий синк повторит
  дефект.
- **Перенести весь `standards/` Хаба.** Отклонено: вносит контракты, у которых в
  споке нет ни применения, ни целей ссылок, — те же битые ссылки в новом месте.
- **Взять клиент Smart Sync Хаба.** Отклонено по ADR-009 (запрет упреждающего
  введения механизма) — при повторяющейся боли решение можно пересмотреть.
- **Автоматизировать синк по расписанию.** Отклонено: issue #265 прямо требует
  односторонний неавтоматический перенос под решением человека.

## Связанные артефакты

- Issue #265: <https://github.com/G-Ivan-A/mango_ba_prompts/issues/265>
- Точка синка: [`.hub-profile.json`](../../.hub-profile.json)
- Скрипт синка: [`scripts/sync_from_hub.py`](../../scripts/sync_from_hub.py)
- Проверка: [`scripts/validate_issue_265_hub_sync.py`](../../scripts/validate_issue_265_hub_sync.py)
- Мост в исследования Хаба: [`docs/hub-research-dependencies.md`](../hub-research-dependencies.md)
- Карта артефактов: [`pr-ops/artifact-map.md`](../../pr-ops/artifact-map.md)
- Предыдущий синк (ADR-0001): [`docs/adr/0001-hub-sync-pr208.md`](0001-hub-sync-pr208.md)
