# Зависимости конвейера извлечения БЗ (issue #111).
# Ставится только при запуске извлечения/генерации фикстуры — НЕ нужно для
# CI-валидатора (scripts/validate_issue_111_kb_pipeline.py — только stdlib).
#
#   python3 -m pip install -r scripts/kb/requirements.txt
#
# Версии — протестированные на этом стенде; патч-обновления безопасны.

# --- Извлечение (scripts/kb/extract.py) ---
pdfplumber==0.11.10   # текст, координаты, кегль шрифта (детект заголовков), таблицы
PyMuPDF>=1.24         # байты встроенных растровых изображений; опционально (есть fallback)
tiktoken>=0.7         # реальный подсчёт токенов (cl100k_base); опционально (есть эвристика)

# --- Генерация синтетической фикстуры (scripts/kb/make_sample_pdf.py) ---
# Нужны ТОЛЬКО чтобы пересобрать тестовый PDF; для извлечения не требуются.
reportlab>=4.0        # сборка PDF из текста/таблиц/изображения
Pillow>=10.0          # отрисовка диаграммы-«скриншота»
