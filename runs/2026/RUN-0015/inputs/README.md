---
status: draft
version: 0.1
updated: 2026-08-21
ai-generated: true
type: input
scope: mango-only
related_issues:
  - "https://github.com/G-Ivan-A/mango_ba_prompts/issues/282"
---

# Вход прогона: история чата по задаче 57204

> **Источник.** Файл, приложенный к
> [issue #282](https://github.com/G-Ivan-A/mango_ba_prompts/issues/282):
> `57204-chat-export-1787301664041.json` (экспорт диалога БА с LLM).
> Прогон **не воспроизводился заново**: фиксируется реально состоявшийся диалог
> (Proof of Execution), артефакты собраны из его содержимого.

## Состав входа

| Файл | Что это |
| --- | --- |
| [`57204-chat-export-1787301664041.json`](57204-chat-export-1787301664041.json) | Дословный экспорт истории чата (251 451 байт), как приложен к issue #282. Не редактировался. |
| [`transcript.md`](transcript.md) | Линейный транскрипт 20 сообщений, полученный из экспорта детерминированным скриптом. Промежуточный артефакт для чтения и цитирования. |

## Как получен транскрипт (воспроизводимо)

```bash
python3 scripts/chat_export_to_markdown.py \
  runs/2026/RUN-0015/inputs/57204-chat-export-1787301664041.json \
  --transcript runs/2026/RUN-0015/inputs/transcript.md \
  --metrics runs/2026/RUN-0015/logs/turn-metrics.md
```

> **Статус скрипта.** `scripts/chat_export_to_markdown.py` — **локальный
> инструмент воспроизводимости, а не артефакт прогона**. Он запускается вручную,
> не вызывается из GitHub Actions и не входит в CI, поэтому остаётся
> работоспособным при отключённых Actions. Зависимостей, кроме стандартной
> библиотеки Python 3, нет. Общее правило — в [`runs/README.md` → «Локальные
> инструменты воспроизводимости»](../../../README.md#локальные-инструменты-воспроизводимости).

Особенности формата экспорта, из-за которых нужен скрипт, а не «просто открыть
JSON»:

- сообщения лежат в `[0].chat.history.messages` словарём по id, линейная ветка
  восстанавливается обходом от `currentId` вверх по `parentId`;
- текст ответа ассистента находится **не** в поле `content` (оно пустое), а в
  элементах `content_list` с `phase == "answer"` (рядом лежит
  `phase == "thinking_summary"`);
- метрики токенов — в `content_list[*].usage`
  (`input_tokens`, `output_tokens`, `output_tokens_details.reasoning_tokens`).

Транскрипт и `logs/turn-metrics.md` — производные файлы: их можно пересобрать из
JSON в любой момент, frontmatter в них намеренно не добавляется, чтобы
пересборка была побайтово детерминированной.

## Бизнес-вход диалога (кратко)

Задача 57204 — **валидация функциональных требований** на доработку модуля
Интеграции Mango Office с AMO CRM: добавление возможности выбрать **IVR-схему**
в качестве условия срабатывания Правила создания Контактов и Сделок для входящих
вызовов (взамен индивидуальной настройки каждого из до 2000 номеров ВАТС при до
10 воронках продаж).

БА подал в диалог:

- глоссарий (Система, AMO CRM / Внешняя система, Интеграция, ЛК, Пользователь);
- черновик ФТ (раздел 2 — задачи, раздел 3 — этапы работ, раздел 4 — ФТ и
  сценарии, раздел 6 — ограничения);
- два приложенных файла: `Макет 1_2.png` (реплика 2) и
  `LK_manual_v-119_compressed.pdf` — руководство пользователя ЛК (реплика 4),
  на которое модель ссылается при проверке терминологии.

Приложенные PNG и PDF в экспорт как отдельные файлы не выгружены (в `files`
реплик присутствуют только их имена), поэтому в `inputs/` не дублируются:
их содержимое доступно только через ссылки модели в транскрипте. Это ограничение
входа зафиксировано в [`../outputs/quality-findings.md`](../outputs/quality-findings.md).
