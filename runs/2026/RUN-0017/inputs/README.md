---
status: draft
version: 0.1
updated: 2026-08-21
ai-generated: true
type: input
scope: mango-only
related_issues:
  - "https://github.com/G-Ivan-A/mango_ba_prompts/issues/270"
---

# Вход прогона: история чата по задаче 1076

> **Источник.** Файл, приложенный к
> [issue #270](https://github.com/G-Ivan-A/mango_ba_prompts/issues/270):
> `1076-chat-export-1787301046512.json` (экспорт диалога БА с LLM).
> Прогон **не воспроизводился заново**: фиксируется реально состоявшийся диалог
> (Proof of Execution), артефакты собраны из его содержимого.

## Состав входа

| Файл | Что это |
| --- | --- |
| [`1076-chat-export-1787301046512.json`](1076-chat-export-1787301046512.json) | Дословный экспорт истории чата (1 115 676 байт), как приложен к issue #270. Не редактировался. |
| [`transcript.md`](transcript.md) | Линейный транскрипт 56 сообщений, полученный из экспорта детерминированным скриптом. Промежуточный артефакт для чтения и цитирования. |

## Как получен транскрипт (воспроизводимо)

```bash
python3 scripts/chat_export_to_markdown.py \
  runs/2026/RUN-0017/inputs/1076-chat-export-1787301046512.json \
  --transcript runs/2026/RUN-0017/inputs/transcript.md \
  --metrics runs/2026/RUN-0017/logs/turn-metrics.md
```

Особенности формата экспорта, из-за которых нужен скрипт, а не «просто открыть
JSON»:

- сообщения лежат в `[0].chat.history.messages` словарём по id, линейная ветка
  восстанавливается обходом от `currentId` вверх по `parentId`;
- текст ответа ассистента находится **не** в поле `content` (оно пустое), а в
  элементах `content_list` с `phase == "answer"` (рядом лежит
  `phase == "thinking_summary"`);
- метрики токенов — в `content_list[*].usage`
  (`input_tokens`, `output_tokens`, `output_tokens_details.reasoning_tokens`).

Транскрипт и `logs/turn-metrics.md` — производные файлы: их можно пересобрать из
JSON в любой момент, frontmatter в них намеренно не добавляется, чтобы
пересборка была побайтово детерминированной.

## Бизнес-вход диалога (кратко)

Задача 1076 — **доработка интеграции MANGO OFFICE с BPMSoft** (включая
конфигурацию «Эстейт»): передача Артефактов ВКС (Транскрибация ВКС, Резюме ВКС /
Конспект) во Внешнюю систему. БА подал в диалог:

- собственный базовый глоссарий (Система, Внешняя система, ВАТС, ВКС, ЛК,
  Пользователь, Сотрудник) — по правилу «защита глоссария» из промпта;
- документ `Mango_office_integration_BPMSoft (1).pdf` — руководство по интеграции
  с BPMSoft (для Шага 0, As-Is);
- документ `UserGuide_Windows_mTalker_ch1_Working.11.06.26.pdf` — руководство
  пользователя MTalker (для проверки фактов по расшифровке/конспекту, п. 7.3);
- `image.png` — образец формата матрицы покрытия сценариев;
- шаблоны формирования требований из смежной задачи (ФТ по дополнительному
  API-ключу, ср. [`RUN-0012`](../../RUN-0012/outputs/final-artifact.md)).

Сами PDF/PNG в экспорт не попали и в репозитории не хранятся: доступны только
цитаты из них внутри диалога. Это ограничивает верификацию фактов документацией
(см. [`../outputs/quality-findings.md`](../outputs/quality-findings.md)).
