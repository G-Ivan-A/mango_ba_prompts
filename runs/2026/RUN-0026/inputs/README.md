---
status: draft
version: 0.1
updated: 2026-08-21
ai-generated: true
type: input
scope: mango-only
related_issues:
  - "https://github.com/G-Ivan-A/mango_ba_prompts/issues/283"
---

# Вход прогона: история чата по задаче 1007

> **Источник.** Файл, приложенный к
> [issue #283](https://github.com/G-Ivan-A/mango_ba_prompts/issues/283):
> `1007-chat-export-1787301730570.json` (экспорт диалога БА с LLM).
> Прогон **не воспроизводился заново**: фиксируется реально состоявшийся диалог
> (Proof of Execution), артефакты собраны из его содержимого.

## Состав входа

| Файл | Что это |
| --- | --- |
| [`1007-chat-export-1787301730570.json`](1007-chat-export-1787301730570.json) | Дословный экспорт истории чата (443 148 байт), как приложен к issue #283. Не редактировался. |
| [`transcript.md`](transcript.md) | Линейный транскрипт 20 реплик, полученный из экспорта детерминированным скриптом. Промежуточный артефакт для чтения и цитирования. |

Нумерация реплик `[0]…[19]` во всех файлах прогона — это индексы из
`transcript.md`; чётные принадлежат БА, нечётные — модели.

## Как получен транскрипт (воспроизводимо)

```bash
python3 scripts/chat_export_to_markdown.py \
  runs/2026/RUN-0026/inputs/1007-chat-export-1787301730570.json \
  --transcript runs/2026/RUN-0026/inputs/transcript.md \
  --metrics runs/2026/RUN-0026/logs/turn-metrics.md
```

> **Статус скрипта.** `scripts/chat_export_to_markdown.py` —
> **локальный инструмент воспроизводимости, а не артефакт прогона**. Запускается
> вручную, не вызывается из GitHub Actions и не входит в CI, поэтому остаётся
> работоспособным при отключённых Actions. Зависимостей, кроме стандартной
> библиотеки Python 3, нет. Общее правило — в
> [`runs/README.md` → «Локальные инструменты
> воспроизводимости»](../../../README.md#локальные-инструменты-воспроизводимости).

Транскрипт и [`logs/turn-metrics.md`](../logs/turn-metrics.md) — производные
файлы: их можно пересобрать из JSON в любой момент, frontmatter в них
намеренно не добавляется, чтобы пересборка была побайтово детерминированной.

## Особенности этой выгрузки

- Ветвлений и регенераций нет: все 20 сообщений лежат в одной линейной цепочке
  от корня до `currentId`, поэтому транскрипт полон — ничего не отброшено.
- Сессия шла в **два захода**: 2026-04-30 14:06–14:45 UTC (реплики [0]…[13]) и
  2026-05-04 09:09–09:24 UTC (реплики [14]…[19]). Календарное окно ≈91.3 ч,
  активное время ≈54 мин — различие зафиксировано в
  [`../logs/turn-metrics.md`](../logs/turn-metrics.md) и в `metadata.yaml`
  (`duration_wall_clock_s` против `duration_active_s`).
- Модель в реплике [7] использовала **встроенные веб-инструменты**: два вызова
  `web_search` и один `web_extractor` (батч из трёх URL). Их результаты в
  транскрипт не попадают — они лежат в `content_list[*].extra.tool_result`
  исходного JSON и разбираются отдельно, см.
  [`../logs/grounding-check.md`](../logs/grounding-check.md).
- **Вложение реплики [14] — скриншот `Макет 1.png` (90 059 байт) — в репозитории
  отсутствует.** В экспорте лежит только метаданные файла и подписанная ссылка
  на CDN провайдера (`cdn.qwenlm.ai/...?key=...`); байты изображения в JSON не
  включены, а ссылка из окружения фиксации недоступна. Следствие: утверждения
  модели о содержимом макета (реплика [15]) в этом прогоне **проверить нельзя** —
  это отмечено в [`../outputs/quality-findings.md`](../outputs/quality-findings.md)
  (Г3) как ограничение доказательной базы, а не как доказанная галлюцинация.
- Пустых ответов и обрывов нет: все 10 реплик модели имеют `phase: "answer"` со
  статусом `finished` и непустым содержимым, у всех есть блок `usage`.

## Бизнес-вход диалога (кратко)

Задача 1007 — доработка **интеграции MANGO OFFICE с amoCRM**: в функции
«Добавить автозвонок в кампанию обзвона» нет опции «Перенести в воронку/этап в
случае дозвона», аналогичной существующей опции блока «Совершать автоперезвон».
Заказчик хочет, чтобы Сделка автоматически переводилась на заданный Этап Воронки
при успешном соединении в кампании исходящего обзвона.

БА подал в диалог:

- в реплике [0] — рамку и правила: роль («команда сертифицированных бизнес-
  аналитиков», TM Forum Frameworx: eTOM, ODA), формат требований («Система должна
  предоставить возможность пользователю…»), запрет расширять функциональность,
  нумерацию `4.x` и трёхшаговый протокол взаимодействия (контекст → оптимизация
  структуры → согласование ФТ);
- в реплике [2] — бизнес-контекст, ссылку на Руководство пользователя по
  интеграции с amoCRM (**стр. 127**, раздел «Настройка интеграции кампании
  исходящего обзвона с digital-воронкой») и глоссарий (Система, AMO CRM,
  Интеграция, ЛК, Пользователь, Воронка/Лид/Сделка) с пометкой, что определение
  «Воронка / Лид / Сделка» надо доработать;
- в реплике [14] — скриншот макета с областью доработки в красном квадрате и
  смену рамки: переформулировать требования «не верхнеуровнево, а под разработку»
  с валидацией через ODA Capability Map.

Сам текст руководства в диалог **не подавался** — только ссылка словами. В
репозитории документ зарегистрирован как источник БЗ
([`kb/sources/integration_amocrm/meta.json`](../../../../kb/sources/integration_amocrm/meta.json),
153 стр.), но PDF в репозиторий не загружен, поэтому проверить стр. 127 нечем —
см. [`../logs/grounding-check.md`](../logs/grounding-check.md).
