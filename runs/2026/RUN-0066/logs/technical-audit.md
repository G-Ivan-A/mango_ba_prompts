---
status: draft
updated: 2026-09-01
ai-generated: true
type: log
---

# Технический аудит RUN-0066

## Метод

1. XLSX прочитан напрямую как Open XML (`zipfile` + `ElementTree`, без внешних
   зависимостей); зафиксированы SHA-256, размер, видимость листов и диапазоны.
2. Все 409 непустых строк A:D перенесены в отчёт без редакторской правки.
   Сверка выполняется скриптом `experiments/issue_351/verify_source_fidelity.py`,
   который перечитывает XLSX и сравнивает ячейки посимвольно.
3. Оценка выполнена поштучно по каждому из 386 требований
   (`experiments/issue_351/assess_part1..4.py`): вердикт, набор evidence-ключей,
   обоснование и независимая аудиторская пометка. Эвристики по ключевым словам
   не применялись.
4. Каждый evidence-ключ указывает только на путь раздела `kb/processed`.
   § , заголовок и страницы подставляются из frontmatter этого раздела в момент
   генерации (`experiments/issue_351/cite.py`), поэтому пагинация верна
   по построению и перепроверяема.
5. Валидатор `scripts/validate_issue_351_run.py` независимо перечитывает БЗ и
   проверяет: ровно 6 колонок, шкалу вердиктов, сплошную нумерацию 1..386,
   непустые обоснование и аудит у каждого требования, существование раздела по
   ссылке и совпадение § , заголовка и страниц с frontmatter.

## Результат

- Оценены все 386 требований: `Да` — 338, `Частично` — 34, `Нет` — 14, пустых — 0.
- Атомарных ссылок на БЗ — 702, все проверены против frontmatter.
- Эскалация в веб (mango-office.ru) использована точечно: реестр российского ПО,
  интеграция со Skillaz, страница информационной безопасности.

## Требования с вердиктом «Нет» (нет подтверждения в SSOT)

№1, 4, 5, 6, 16, 30, 50, 78, 173, 189, 244, 367, 378, 383 — организационные,
договорные и количественные обязательства (выделенная команда, сроки, объём
номерной ёмкости, число внедрений, нагрузочные испытания, антивирусная
архитектура, программа обучения). Пользовательская документация такие
обязательства подтвердить не может; нужны договорные документы и отчёты.

## Требования с вердиктом «Частично» (декомпозиция даёт разные ответы)

№7, 40, 49, 51, 53, 54, 55, 56, 61, 77, 155, 164, 176, 187, 190, 211, 225, 226,
236, 297, 299, 301, 302, 306, 310, 320, 328, 340, 348, 358, 359, 361, 363, 377.
Во всех случаях в колонке 5 явно указано, какая атомарная часть подтверждена, а
какая не документирована. `Частично` не ставится там, где требование атомарно.

## Блок-факторы

Исходное значение колонки C `Является блок-фактором?` сохранено в колонке 6
пометкой `БЛОК-ФАКТОР`. Подтверждённая capability не снимает необходимость
продуктового решения по таким строкам.

## Gate решения

До коммерческого обязательства требуются: product review по каждому блок-фактору,
архитектурный L2/L3 по интеграции со Skillaz, security/legal review по ПДн и
записям разговоров, договорный SLA и отчёты нагрузочного тестирования.
