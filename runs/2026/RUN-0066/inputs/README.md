---
status: draft
updated: 2026-09-01
ai-generated: true
type: evidence
---

# Входы RUN-0066

Источник — вложение issue [#351](https://github.com/G-Ivan-A/mango_ba_prompts/issues/351),
локально сохранённое как `experiments/issue_351/requirements.xlsx`. По контракту
прогонов исходный XLSX в репозитории не хранится: оригиналы остаются на рабочей
станции пользователя, здесь фиксируется только контрольная сумма.

| Файл | Размер, байт | SHA-256 | Структура |
| --- | ---: | --- | --- |
| `requirements.xlsx` | 51 690 | `aa1d732e28256c043d39da5683f064993bea8b2ae30fc40ba4e64edb457b2103` | `Требования к системе` — диапазон A1:D414, 409 непустых строк A:D; `Лист1` — hidden, 3 значения статусов |

Файл отличается от источника RUN-0065 (`4806288a…4287ba`, 53 090 байт): в новой
версии отсутствует лист `SLA поддержки`. Скрытый лист `Лист1` задаёт словарь
исходных статусов (`Реализовано`, `Доработка`, `Реализация невозможна`) и
доказательством не является.

## Отображение колонок источника на колонки отчёта

Отчёт содержит ровно шесть колонок, поэтому исходные поля отображаются так:

| Колонка XLSX | Заголовок в источнике | Колонка отчёта |
| --- | --- | --- |
| A | `№ требования` | 1 — `№` |
| B | `Требования к системе` | 2 — `Требование` |
| C | `Является блок-фактором?` | значение сохраняется в колонке 6 как пометка `БЛОК-ФАКТОР (исходная колонка C: «…»)` |
| D | `Факт реализации` | 3 — `Комментарий участника` |

Колонка C не выбрасывается: её исходное значение переносится в технический аудит,
чтобы не потерять данные источника и не нарушить требование о шести колонках.

## Лог сверки колонок 1–3 с источником

Скрипт `experiments/issue_351/verify_source_fidelity.py` повторно читает XLSX и
посимвольно сравнивает его с таблицей отчёта:

```
источник: requirements.xlsx, 51690 байт, sha256 aa1d732e28256c043d39da5683f064993bea8b2ae30fc40ba4e64edb457b2103
строк источника (непустых A/B/D): 409
строк в отчёте: 409
ячеек, отличающихся только краевыми пробелами/переводами строки: 82
колонки 1-3 совпадают с источником посимвольно (краевые пробелы markdown-таблица не переносит): расхождений нет
```

Единственная нормализация — краевые пробелы, неразрывные пробелы и переводы
строки в начале и конце ячейки: markdown-таблица их не переносит. Внутренние
пробелы, неразрывные пробелы (`\xa0`), опечатки и нумерация источника сохранены
без изменений, внутренние переводы строки представлены как `<br>`.
