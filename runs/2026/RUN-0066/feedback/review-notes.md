---
status: draft
updated: 2026-09-01
ai-generated: true
type: feedback
---

# Заметки к ревью RUN-0066

## Что проверять в первую очередь

1. Колонки 1–3: сверка запускается локально при наличии исходного XLSX —
   `python3 experiments/issue_351/verify_source_fidelity.py`. Лог сверки
   зафиксирован в `inputs/README.md`.
2. Пагинация: `python3 scripts/validate_issue_351_run.py` перечитывает
   frontmatter каждого раздела БЗ по ссылке и падает при расхождении § ,
   заголовка или страниц. Этот же валидатор подхватывается `make validate-full`.
3. A/B-сравнение с RUN-0065 — `logs/ab-comparison.md`.

## Открытые вопросы к владельцу продукта

- Требования с вердиктом `Нет` (№1, 4, 5, 6, 16, 30, 50, 78, 173, 189, 244, 367,
  378, 383) закрываются не документацией, а договорными документами, отчётами
  нагрузочного тестирования и референс-листом внедрений. Нужны эти артефакты.
- Количественные пороги (300 быстрых ответов, 100 SMS в пакете, 24 месяца
  хранения, минимальные версии ОС, SLA 99,9%) в БЗ не документированы — по ним
  выставлено `Частично`. Требуется подтверждение из эксплуатационных регламентов.
- Исходный XLSX issue #351 не содержит листа `SLA поддержки`, который был в
  источнике RUN-0065. Нужно подтвердить, что SLA выведен из ТЗ намеренно.

## Ограничения прогона

- Оценка выполнена строго по `kb/processed` с точечной эскалацией на
  mango-office.ru; общие знания модели не использовались.
- Краевые пробелы и переводы строки в ячейках markdown-таблица не переносит —
  это единственная нормализация, она задокументирована в `inputs/README.md`.
