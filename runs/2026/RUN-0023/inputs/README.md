---
status: draft
version: 0.1
updated: 2026-08-21
ai-generated: true
type: input
scope: mango-only
related_issues:
  - "https://github.com/G-Ivan-A/mango_ba_prompts/issues/278"
---

# Вход прогона: история чата по задаче 59295

> **Источник.** Файл, приложенный к
> [issue #278](https://github.com/G-Ivan-A/mango_ba_prompts/issues/278):
> `59295-chat-export-1787301537378.json` (экспорт диалога БА с LLM).
> Прогон **не воспроизводился заново**: фиксируется реально состоявшийся диалог
> (Proof of Execution), артефакты собраны из его содержимого.

## Состав входа

| Файл | Что это |
| --- | --- |
| [`59295-chat-export-1787301537378.json`](59295-chat-export-1787301537378.json) | Дословный экспорт истории чата (86 887 байт), как приложен к issue #278. Не редактировался. |
| [`transcript.md`](transcript.md) | Линейный транскрипт 6 сообщений (3 user + 3 assistant), полученный из экспорта детерминированным скриптом. Промежуточный артефакт для чтения и цитирования. |

## Как получен транскрипт (воспроизводимо)

```bash
python3 scripts/chat_export_to_markdown.py \
  runs/2026/RUN-0023/inputs/59295-chat-export-1787301537378.json \
  --transcript runs/2026/RUN-0023/inputs/transcript.md \
  --metrics runs/2026/RUN-0023/logs/turn-metrics.md
```

> **Статус скрипта.** `scripts/chat_export_to_markdown.py` — **локальный
> инструмент воспроизводимости, а не артефакт прогона**. Он запускается вручную,
> не вызывается из GitHub Actions и не входит в CI. Зависимостей, кроме
> стандартной библиотеки Python 3, нет. Общее правило — в [`runs/README.md` →
> «Локальные инструменты
> воспроизводимости»](../../../README.md#локальные-инструменты-воспроизводимости).

Транскрипт и `logs/turn-metrics.md` — производные файлы: их можно пересобрать из
JSON в любой момент, frontmatter в них намеренно не добавляется, чтобы
пересборка была побайтово детерминированной. Особенности формата экспорта
(ветка восстанавливается от `currentId` по `parentId`, текст ответа лежит в
`content_list[*]` с `phase == "answer"`, метрики — в `content_list[*].usage`)
описаны в [`RUN-0017/inputs/README.md`](../../RUN-0017/inputs/README.md).

## Бизнес-вход диалога (кратко)

Задача 59295 — **валидация исходных ФТ** по доработке модуля «Обращения»
омниканального КЦ: у Заказчика входящие письма клиентов попадают в КЦ через
общий корпоративный ящик, поэтому отправителем обращения оказывается этот ящик,
а не клиент; ответить конечному клиенту напрямую невозможно. Требуется
функционал «Переслать» (forward) в карточке e-mail-обращения.

БА подал в диалог:

- реплика [0] — промпт `mango-fr-validation-legacy` **дословно**
  ([`prompts/fr-validation-legacy.md`](../../../../prompts/fr-validation-legacy.md),
  v1.0); совпадение с файлом репозитория проверено программно, см.
  [`../outputs/prompts-chain.md`](../outputs/prompts-chain.md);
- реплика [2] — глоссарий (Система, КЦ, Клиент, Менеджер), разделы 1–6 исходного
  документа: проблема/цель/задачи, описание решения, ФТ 4.1–4.4, пустой раздел
  НФТ и особенности реализации 6.1.1–6.1.7;
- реплика [4] — точечное дополнение: новый пункт 6.1.8 и содержательная
  поправка к нему.

**Вложений в диалоге нет:** во всех трёх пользовательских репликах экспорта
`files = []`. Никакая документация продукта (в том числе «Руководство
пользователя КЦ», упомянутое человеком в [4]) в контекст модели не подавалась —
это ограничивает верифицируемость её ссылок на источники (см.
[`../outputs/quality-findings.md`](../outputs/quality-findings.md)).
