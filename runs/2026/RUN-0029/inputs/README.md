---
status: draft
version: 0.1
updated: 2026-08-21
ai-generated: true
type: input
scope: mango-only
related_issues:
  - "https://github.com/G-Ivan-A/mango_ba_prompts/issues/281"
---

# Вход прогона: история чата по задаче 58093

> **Источник.** Файл, приложенный к
> [issue #281](https://github.com/G-Ivan-A/mango_ba_prompts/issues/281):
> `58093-chat-export-1787301612928.json` (экспорт диалога БА с LLM).
> Прогон **не воспроизводился заново**: фиксируется реально состоявшийся диалог
> (Proof of Execution), артефакты собраны из его содержимого.

## Состав входа

| Файл | Что это |
| --- | --- |
| [`58093-chat-export-1787301612928.json`](58093-chat-export-1787301612928.json) | Дословный экспорт истории чата (453 173 байта), как приложен к issue #281. Не редактировался. |
| [`transcript.md`](transcript.md) | Линейный транскрипт 38 реплик, полученный из экспорта детерминированным скриптом. Промежуточный артефакт для чтения и цитирования. |

## Как получен транскрипт (воспроизводимо)

```bash
python3 scripts/chat_export_to_markdown.py \
  runs/2026/RUN-0029/inputs/58093-chat-export-1787301612928.json \
  --transcript runs/2026/RUN-0029/inputs/transcript.md \
  --metrics runs/2026/RUN-0029/logs/turn-metrics.md

python3 experiments/parse_58093_chat_export.py \
  runs/2026/RUN-0029/inputs/58093-chat-export-1787301612928.json
```

> **Статус скриптов.** `scripts/chat_export_to_markdown.py` и
> `experiments/parse_58093_chat_export.py` — **локальные инструменты
> воспроизводимости, а не артефакты прогона**. Они запускаются вручную, не
> вызываются из GitHub Actions и не входят в CI, поэтому остаются
> работоспособными при отключённых Actions. Зависимостей, кроме стандартной
> библиотеки Python 3, нет. Общее правило — в [`runs/README.md` → «Локальные
> инструменты воспроизводимости»](../../../README.md#локальные-инструменты-воспроизводимости).

Особенности формата экспорта, из-за которых нужен скрипт, а не «просто открыть
JSON»:

- сообщения лежат в `[0].chat.history.messages` словарём по id, линейная ветка
  восстанавливается обходом от `currentId` вверх по `parentId`;
- текст ответа ассистента находится **не** в поле `content` (оно пустое), а в
  элементах `content_list` с `phase == "answer"` (рядом лежит
  `phase == "thinking_summary"`);
- метрики токенов — в `content_list[*].usage`
  (`input_tokens`, `output_tokens`, `output_tokens_details.reasoning_tokens`).

Транскрипт и `logs/turn-metrics.md` — производные файлы: их можно пересобрать из
JSON в любой момент, frontmatter в них намеренно не добавляется, чтобы
пересборка была побайтово детерминированной.

## Вложения диалога: что в экспорт НЕ попало

В диалоге два вложения, и **ни одно из них не сохранено внутри экспорта** — в
JSON лежат только имя, размер и подписанная ссылка на внешний CDN
(`cdn.qwenlm.ai`, параметр `key=` с ограниченным сроком жизни):

| Реплика | Вложение | Размер | Что с ним |
| --- | --- | --- | --- |
| 0 | `Mango_office_integration_amoCRM.pdf` | 8 867 257 байт | Официальная инструкция по интеграции MANGO OFFICE ↔ amoCRM. Содержимого в экспорте нет. |
| 4 | `Макет 1.png` | 98 605 байт | Макет доработки экрана настроек в ЛК. Содержимого в экспорте нет. |

**Следствие для анализа.** Все ссылки модели на пункты инструкции
(«п. 7.2», «п. 2.12», «п. 14.2», «разделы 2.4, 2.5, 2.23, 2.24», «официальная
инструкция v25.08.2025») и на макет **не проверяемы по архиву прогона**. Они
разбираются в [`../outputs/quality-findings.md`](../outputs/quality-findings.md)
как непроверяемые утверждения, а не как подтверждённые или как галлюцинации.

## Бизнес-вход диалога (кратко)

Задача 58093 — доработка интеграции MANGO OFFICE ↔ amoCRM: **при входящем звонке
открывать Карточку сделки клиента в новой вкладке браузера**, без промежуточного
клика по мини-карточке звонка.

БА подал в диалог:

- исходное описание проблемы от Заказчика (пункты 2–5: объект изменения, текущее
  состояние, желаемое изменение, причина);
- собственную редакцию раздела 2 документации (Проблема / Цель / Задачи);
- PDF официальной инструкции по интеграции (реплика 0);
- ad-hoc-промпт «команда сертифицированных бизнес-аналитиков… eTOM / ODA
  Capability Map» с правилами формулировок и тремя шагами (реплика 2);
- макет экрана настроек и точный путь к настройке в ЛК (реплика 4).
