---
status: draft
version: 0.1
updated: 2026-08-21
ai-generated: true
type: input
scope: mango-only
related_issues:
  - "https://github.com/G-Ivan-A/mango_ba_prompts/issues/268"
---

# Вход: экспорт истории чата «1059 -2»

> **Источник.** Файл истории чата, приложенный к
> [issue #268](https://github.com/G-Ivan-A/mango_ba_prompts/issues/268):
> [`1059_2-chat-export-1787300919840.json`](https://github.com/user-attachments/files/31297233/1059_2-chat-export-1787300919840.json)
> (4 638 665 байт, 279 сообщений). Сам файл в репозиторий **не копируется**:
> он содержит выгрузку рабочей переписки БА с Заказчиком-контекстом и весит
> ~4,6 МБ; контракт `runs/` допускает ссылку на вход (issue #268, раздел
> «Обязательные элементы»: «`inputs/` (ссылка или копия прикрепленного файла)»).

## Что внутри файла

| Параметр | Значение |
| --- | --- |
| Заголовок чата | `1059 -2` |
| Всего сообщений в экспорте | 279 (с учётом отброшенных веток регенерации) |
| Активная ветка (`history.currentId` → `parentId`) | 242 сообщения: 121 реплика БА + 121 ответ модели |
| Период | 2026-06-18 15:22 UTC — 2026-07-31 14:19 UTC |
| Модели | `qwen3.7-plus` (101 ответ), `qwen3.8-max-preview` (20 ответов) |
| Токены (сумма по `usage` всех ответов) | вход 79 405 522, выход 543 180 |

## Структура экспорта (как читать)

- Верхний уровень — список; рабочий объект — `[0].chat`.
- Сообщения лежат в `chat.history.messages` — словарь по `id`, связанный
  через `parentId` / `childrenIds`; активная ветка восстанавливается обходом
  от `chat.history.currentId` вверх по `parentId`.
- У реплик БА текст в `content`; у ответов модели `content` пустой, а текст
  собирается из `content_list[]`, где `phase == "answer"` (прочие фазы:
  `thinking_summary`, `web_extractor`, `bio`).
- Метрики токенов — в `content_list[].usage.input_tokens` / `output_tokens`.

Воспроизводимый разбор и подсчёт метрик:
[`experiments/analyze_chat_metrics.py`](../../../../experiments/analyze_chat_metrics.py)
(на вход подаётся скачанный JSON).

## Оговорка о статусе данных (из issue #268)

> Данные в файле могут представлять как финальные, так и промежуточные
> результаты. Результаты согласований **не являются** согласованными шаблонами
> или golden case. Прогоны фиксируются для сбора статистики и анализа БА:
> процессов, операций, результативности промптов, успешных результатов и
> галлюцинаций.

Поэтому [`outputs/final-artifact.md`](../outputs/final-artifact.md) — это
**зафиксированное на 2026-07-31 состояние документа БА**, а не утверждённое
Заказчиком ТЗ и не эталон для копирования.
