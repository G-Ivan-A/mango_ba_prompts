---
status: draft
version: 0.1
updated: 2026-08-21
ai-generated: true
type: input
scope: mango-only
related_issues:
  - "https://github.com/G-Ivan-A/mango_ba_prompts/issues/277"
---

# Вход прогона: история чата по задаче 1020

> **Источник.** Файл, приложенный к
> [issue #277](https://github.com/G-Ivan-A/mango_ba_prompts/issues/277):
> `1020-chat-export-1787301522802.json` (экспорт диалога БА с LLM).
> Прогон **не воспроизводился заново**: фиксируется реально состоявшийся диалог
> (Proof of Execution), артефакты собраны из его содержимого.

## Состав входа

| Файл | Что это |
| --- | --- |
| [`1020-chat-export-1787301522802.json`](1020-chat-export-1787301522802.json) | Дословный экспорт истории чата (490 630 байт), как приложен к issue #277. Не редактировался. |
| [`transcript.md`](transcript.md) | Линейный транскрипт 8 реплик, полученный из экспорта детерминированным скриптом. Промежуточный артефакт для чтения и цитирования. |

Нумерация реплик `[0]…[7]` во всех файлах прогона — это индексы из
`transcript.md`; чётные принадлежат БА, нечётные — модели.

## Как получен транскрипт (воспроизводимо)

```bash
python3 scripts/chat_export_to_markdown.py \
  runs/2026/RUN-0024/inputs/1020-chat-export-1787301522802.json \
  --transcript runs/2026/RUN-0024/inputs/transcript.md \
  --metrics runs/2026/RUN-0024/logs/turn-metrics.md
```

> **Статус скрипта.** `scripts/chat_export_to_markdown.py` —
> **локальный инструмент воспроизводимости, а не артефакт прогона**. Запускается
> вручную, не вызывается из GitHub Actions и не входит в CI, поэтому остаётся
> работоспособным при отключённых Actions. Зависимостей, кроме стандартной
> библиотеки Python 3, нет. Общее правило — в
> [`runs/README.md` → «Локальные инструменты
> воспроизводимости»](../../../README.md#локальные-инструменты-воспроизводимости).

Транскрипт и [`logs/turn-metrics.md`](../logs/turn-metrics.md) — производные
файлы: их можно пересобрать из JSON в любой момент, frontmatter в них
намеренно не добавляется, чтобы пересборка была побайтово детерминированной.

## Особенности этой выгрузки

- Ветвлений и регенераций нет: все 8 сообщений лежат в одной линейной цепочке
  от корня до `currentId`, поэтому транскрипт полон — ничего не отброшено.
- Вложенных файлов у реплик нет (`files=[]` во всех заголовках транскрипта):
  весь вход подан текстом.
- Модель в реплике [1] использовала **встроенные веб-инструменты**: два вызова
  `web_extractor` и один `web_search` (в сумме 3 вызова, 132 569 символов
  результатов). Их результаты в транскрипт не попадают — они лежат в
  `content_list[*].extra.tool_result` исходного JSON и разбираются отдельно, см.
  [`../logs/grounding-check.md`](../logs/grounding-check.md).
- Последняя реплика модели [7] имеет `phase: "answer"` со **статусом
  `finished` и пустым содержимым** — то есть ответ формально завершён, но пуст;
  это не обрыв соединения. Блока `usage` у неё нет, поэтому в метрики токенов
  она не входит.

## Бизнес-вход диалога (кратко)

Задача 1020 — **интеграция телефонии MANGO OFFICE с сервисом OkDesk** (учёт
сервисных заявок). Бизнес-потребность Заказчика: любой звонок (внешний **или
внутренний**) должен приводить к созданию заявки в OkDesk с проброшенной
записью разговора. Действующая интеграция партнёра «Простые звонки» этот
сценарий не закрывает.

БА подал в диалог одной репликой [0]:

- роль и рамку («команда сертифицированных бизнес-аналитиков», TM Forum
  Frameworx: eTOM, ODA) и правила вывода (уровень бизнес-логики, формальный
  язык требований, не расширять функциональность, терминология телекома /
  MANGO OFFICE);
- описание кейса Заказчика на языке бизнес-потребности;
- две ссылки на публичную документацию API OkDesk (создание заявки, создание
  записи о телефонном разговоре);
- **собственный черновик пяти вопросов** Заказчику — с просьбой отредактировать
  формулировки и дополнить своими.

Содержимое страниц документации OkDesk в репозитории не хранится: доступны
только результаты веб-инструментов внутри экспорта. Именно они и проверяются в
[`../logs/grounding-check.md`](../logs/grounding-check.md).
