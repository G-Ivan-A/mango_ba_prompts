---
status: draft
version: 0.1
updated: 2026-08-28
ai-generated: true
type: artifact
scope: mango-only
related_issues:
  - "https://github.com/G-Ivan-A/mango_ba_prompts/issues/336"
related_artifacts:
  - "runs/2026/RUN-0061/outputs/link-review-statistics.md"
---

# Ограничения чтения прогона RUN-0061

- **Шкала.** Прогон `statistics`: успех читается по покрытию и сопоставимости выборки, а не по качеству артефакта. Смешивать с прогонами `execution` при выводах о промптах нельзя.
- **Приёмка.** Выраженной приёмки БА в экспорте нет, поэтому `status: draft`, а `success_rate` намеренно не выставлен — оснований в данных нет.
- **Разметка операций** эвристическая (ключевые слова), даёт порядок величин, а не точную классификацию; одна реплика может попасть в несколько категорий.
- **Токены входа** просуммированы по вызовам и включают переотправку контекста: 723648 при максимуме 74559 за один вызов — это не объём переписки.
- **Реплика 16 как граница** выбрана по содержанию (БА отклонил формат ответов); это разметка человеком, а не свойство данных.
- **Сопоставление разделов** сравнивает текст с заголовками страницы, а не смысл: вердикт «на странице не найдено» означает, что раздел с таким названием не публикуется, но не доказывает, что утверждение не подтверждается страницей.
- **Границы поиска по вики.** Проверялись только страницы, названные отчётом и диалогом (79 + индекс). Полный обход `wiki.twin24.ai` не выполнялся: `sitemap.xml` недоступен. Вывод о завершении звонка (раздел 6 [`../outputs/link-review-statistics.md`](../outputs/link-review-statistics.md)) — о проверяемости ссылки, а не о содержании продукта.
- **Состояние вики зафиксировано на 2026-08-28**: заголовки и якоря могли измениться позже; снимок страниц лежит в локальном кэше и в репозиторий не коммитится.
- **Содержимое диалога** в записи отсутствует: восстановить его можно только из исходного вложения issue по провенансу в [`../inputs/README.md`](../inputs/README.md).
